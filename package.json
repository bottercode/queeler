{
  "name": "my-app",
  "version": "1.0.0",
  "description": "GraphQL Express and React app on Render",
  "scripts": {
    "client": "cd client && npm install && npm run start",
    "api": "cd api && npm install && npm run dev",
    "build": "concurrently \"npm run api\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prisma": "^5.5.2"
  },
  "dependencies": {
    "@prisma/client": "^5.5.2"
  }
}
